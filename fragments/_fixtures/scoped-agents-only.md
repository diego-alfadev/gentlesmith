---
scope: agents
---
# Agents-only fixture

This fragment is scoped to the `agents` target. It must appear when rendered
for `agent: agents` and must be absent from any other target (e.g. claude, codex).
