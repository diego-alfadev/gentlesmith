# Context7 Integration

When Context7 MCP is available, verify library and framework APIs before asserting syntax, behavior, or configuration.

- Use Context7 for current library documentation when API behavior may have changed.
- Prefer primary documentation over examples copied from memory.
- If Context7 is unavailable, say so and fall back to project code or official docs.
