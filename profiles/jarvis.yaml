name: jarvis
description: Full stack — system-aware, opinionated, mentor mode. Persona + operative rules + system context. Does NOT include engram protocol or SDD orchestrator (gentle-ai territory).
include:
  - persona/jarvis
  - persona/karpathy-guidelines
  - rules/safety
  - rules/workflow
  - rules/commits
  - rules/tools
  - env/system
  - env/toolchain
  - env/deployment
  - env/secrets

# Optional: declare task-level skills to install via Vercel's `npx skills`.
# gentlesmith forwards these on --apply; it does NOT maintain a skill registry.
# See https://skills.sh for the package directory.
# skills:
#   - vercel-labs/react-19
#   - vercel-labs/a11y-review
